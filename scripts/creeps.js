MyGame.components.creeps = (function(){
	var Creep = function(){
		//needs to know what it looks like
		//needs to know where it is
		//needs to know where it needs to go
			//needs to know how to get there

		function update(elapsedTime){
			//update position
		}

		function draw(elapsedTime){
			//update sprite
			//call draw on sprite
		}

		return {

		};
	}

	return {
		Creep:Creep
	};
}(););
